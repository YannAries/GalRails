Metrics/LineLength:
  Max: 100

############################ Configuration for rubocop# in .rubocop.yml # Most of these are disabling existing cops, primarily# due to a smattering of different styles and loose# guidlines for contributions.## Any of these may be changed.StringLiterals:
  Enabled: false

Style/Tab:
  Enabled: false

config/routes.rb:15:80: C: Line is too long. [80/79]
  # Example of named route that can be invoked with purchase_url(id: product.id)

app/controllers/application_controller.rb:1:1: C: Missing top-level class documentation comment.
class ApplicationController < ActionController::Base
^^^^^

Metrics/LineLength:
  # This will disable the rule completely, regardless what other options you put
  Enabled: false
  # Change the default 80 chars limit value
  Max: 120
  # If you want the rule only apply to a specific folder/file
  Include:
    - 'app/**/*'
  # If you want the rule not to apply to a specific folder/file
  Exclude:
    - 'db/schema.rb'


